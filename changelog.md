## 1.1.2

### Изменения

- Ярлык трафика переименован в «Трафик БС» (Белый список) на всех языках — с подсказкой при наведении о том, что это трафик с мобильного устройства

## 1.1.1

### Исправления

- Группы прокси теперь автоматически обновляются после добавления подписки — больше не нужно нажимать «Обновить» вручную
- Геоданные runetfreedom теперь вшиты в установщик — приложение не скачивает их при первом запуске

## 1.1.0

### Новое

- **Страница Диагностика** — анализ закрытых соединений в реальном времени:
  - Клик по процессу или домену/IP выделяет ячейку и показывает кнопку действия справа
  - Прямо из диагностики добавляй процесс, домен или IP в «Сайты через VPN» или «Сайты в обход VPN»
  - Цветная точка на процессе/домене показывает, что правило уже добавлено (бирюзовый — VPN, красный — обход)
  - Два уровня ошибок: **нет связи** (красный, TCP-провал) и **нет ответа** (жёлтый, запрос ушёл но ответ не пришёл)
  - Фильтры: Все / Ошибки / Напрямую / VPN — с счётчиком в каждой вкладке
  - Сортировка: по времени (новые/старые), по процессу, по хосту, ошибки наверх
  - Toast-уведомление после добавления правила с кнопкой быстрого перехода в «Мои правила»

- **Правила для IP-адресов** — новый тип правил на основе IP-CIDR:
  - IPv4 → `IP-CIDR,x.x.x.x/32` с флагом `no-resolve`
  - IPv6 → `IP-CIDR6,.../128` с флагом `no-resolve`
  - Управление IP-правилами доступно в «Мои правила» (новые разделы) и из диагностики

- **Переводы «Мои правила»** — все элементы страницы переведены на английский и китайский язык

## 1.0.1

### Новое
- Страница **Мои правила** — добавляй домены и приложения через VPN прямо из интерфейса без редактирования конфигов
- Голосовые звонки Discord теперь работают в режиме «Только заблокированные»

### Исправления
- Исправлен сбой интерфейса при повторном добавлении подписки
- Исправлена загрузка сайтов с fake-ip DNS (thangs.com и подобные)
- Исправлен порядок инициализации состояния geodataProgress

## 1.0.6

### Новое
- **Режим Эксперта** в настройках — скрывает технические разделы (Группы прокси, Подключения, Правила, Логи) для простых пользователей; включается при необходимости
- Новые элементы интерфейса переведены на английский и китайский язык

### Исправления
- Кнопка «Поддержка» на главном экране теперь всегда ведёт на Telegram-бот

## 1.0.5

### Новое
- Импорт списка в «Мои правила» — вставь сразу несколько доменов или процессов через кнопку загрузки (каждый с новой строки или через запятую)
- Массовое удаление в «Мои правила» — режим выбора позволяет отметить нужные записи и удалить их разом; есть кнопка «Выбрать всё»

## 1.0.4

### Новое
- Исключения из VPN — добавляй сайты и приложения, которые всегда идут напрямую (мимо VPN)
- Выбор процесса из списка активных подключений одной кнопкой

### Исправления
- Правила исключений теперь корректно перекрывают гео-базы (ru-blocked и др.)

## 1.0.3

### Новое
- Поддержка диплинка `velumvpn://add/ССЫЛКА` — кнопка быстрого добавления подписки прямо из личного кабинета
- Исправлена ссылка на GitHub-репозиторий в настройках
- Кнопка «Поддержка» на главном экране теперь всегда ведёт на Telegram-бот

## 1.0.0

### Первый релиз VelumVPN
- Три режима маршрутизации: только заблокированные, все зарубежные, всё через VPN
- Геоданные от runetfreedom с актуальными базами заблокированных ресурсов РФ
- Автозагрузка геоданных при первом запуске с прогресс-баром
- Поддержка Windows и Linux
- Автообновление приложения
